typeattribute vendor_init data_between_core_and_vendor_violators;

allow vendor_init {
  system_data_file
  tombstone_data_file
  nfc_data_file
}:dir { create_dir_perms relabelfrom };

allow vendor_init proc_decimal:file { write };
allow vendor_init vendor_persist_file:lnk_file read;
allow vendor_init proc_touchpanel:file { write };
allow vendor_init block_device:lnk_file setattr;
allow vendor_init proc_fw_update:file { write };

set_prop(vendor_init, vendor_freq_prop);
set_prop(vendor_init, vendor_camera_prop);

# Allow setting properties
allow vendor_init vendor_cgroup_follow_prop:property_service set;

allow fsck mnt_vendor_file:file { search };
allow hal_fingerprint_default vendor_default_prop:property_service set;
allow rild rm_vendor_radio_vendor_data_file:file { getattr };
allow vendor_init gsi_metadata_file:file { search };
allow vendor_init proc_cmdline:file { getattr };
allow vendor_init rm_vendor_radio_vendor_data_file:file { read };
allow vendor_rmt_storage default_prop:file { read };
allow vendor_rmt_storage system_prop:file { read };
allow vendor_ssr_setup sysfs:file { open };

